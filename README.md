# SmartDesk-Router
An intelligent Support Router created to automatically classify and assign tickets using NLP.

## To Run
- npm install
- cd frontend && npm start
- (parallely) cd classifiers && python runner.py api
